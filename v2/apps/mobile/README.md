# Mobile app — deferred from v2.0

Native iOS/Android phone scanner is **not in v2.0 scope**. The web getUserMedia + Supabase Realtime path covers the paired-phone-as-camera flow well enough for now.

Revisit when:
- v2.0 is stable and shipped.
- A real demand signal exists from shop owners using the embed widget.
- We have buffer for App Store + Play Console review queues.

Plan when revived (Expo, React Native, NativeWind) lives in `../../V2_PLAN.md` git history if needed.
